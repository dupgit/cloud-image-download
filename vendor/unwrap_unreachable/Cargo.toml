# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.58.1"
name = "unwrap_unreachable"
version = "0.1.1"
authors = ["Olivier Delhomme <olivier.delhomme@free.fr>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Provides `Option::unreachable()` and `Result::unreachable()` methods"
documentation = "https://docs.rs/unwrap_unreachable"
readme = "README.md"
keywords = [
    "unwrap",
    "unreachable",
    "UnwrapUnreachable",
]
categories = ["rust-patterns"]
license = "MIT"
repository = "https://github.com/dupgit/unwrap_unreachable"

[lib]
name = "unwrap_unreachable"
path = "src/lib.rs"

[[test]]
name = "tests"
path = "tests/tests.rs"

[dependencies]
